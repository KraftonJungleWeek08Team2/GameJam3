using UnityEngine;

/// <summary>
/// 콤보관리. 
/// </summary>
public class BeatBarResultSystem : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
